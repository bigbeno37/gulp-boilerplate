$(document).ready(function(){
	$("body").css({ "height": $(window).height(), "width": $(window).width() });
	$(".centerTextBody").css({ "height": $(window).height(), "width": $(window).width() });
});